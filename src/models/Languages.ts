


export class Languages {
    code: string;
    name: string;
    native: string;
    rtf: string;

    constructor(Code: string, Name: string, Native: string, Rtf: string) {
        this.code = Code;
        this.name = Name;
        this.native = Native;
        this.rtf = Rtf;
    }
}